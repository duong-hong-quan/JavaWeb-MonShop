<?xml version="1.0" encoding="UTF-8"?>
<project name="JavaWeb-MonShop" default="build" basedir=".">
    <description>Builds, tests, and runs the project JavaWeb-MonShop.</description>

    <!-- Set the properties -->
    <property name="src.dir" value="src"/>
    <property name="build.dir" value="build"/>
    <property name="dist.dir" value="dist"/>

    <!-- Clean target -->
    <target name="clean">
        <delete dir="${build.dir}"/>
        <delete dir="${dist.dir}"/>
    </target>

    <!-- Compile target -->
    <target name="compile" depends="clean">
        <mkdir dir="${build.dir}"/>
        <javac srcdir="${src.dir}" destdir="${build.dir}"/>
    </target>

    <!-- Jar target -->
    <target name="jar" depends="compile">
        <mkdir dir="${dist.dir}"/>
        <jar destfile="${dist.dir}/JavaWeb-MonShop.jar" basedir="${build.dir}"/>
    </target>

    <!-- Build target -->
    <target name="build" depends="jar">
        <echo message="Build completed!"/>
    </target>
</project>
